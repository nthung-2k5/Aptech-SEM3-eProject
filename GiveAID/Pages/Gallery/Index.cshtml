@page
@model GalleryModel
@{
    ViewData["Title"] = "Gallery";
}

<div class="min-h-screen bg-[#F6F1E9]">
    <hero-component title="Gallery" subtitle="Moments from our programmes across communities"/>

    <div class="max-w-7xl mx-auto px-6 py-12">
        <gallery-board filter="@Model.Filter" />
    </div>
</div>
