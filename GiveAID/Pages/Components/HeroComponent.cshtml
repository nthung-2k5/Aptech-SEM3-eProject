@model HeroComponent

<section class="bg-[#1A5C6B] text-white py-12">
    <div class="max-w-7xl mx-auto px-6">
        <p class="text-[#E8C07D] text-xs font-semibold uppercase tracking-widest mb-2">@Model.Subtitle</p>
        <h1 class="text-4xl">@Model.Title</h1>
    </div>
</section>